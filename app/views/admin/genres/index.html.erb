<div class = "container">
      <h1>ジャンル一覧・追加</h1>

      <%= form_with model: @genre, url: admin_genres_path, local:true do |f| %>
         <div class = "field">
           <%= f.label :name,"ジャンル名" %>
           <%= f.text_field :name %>
           <%= f.submit '新規登録', class: "btn-lg btn-success" %>
         </div>
    　　<% end %>

<div class = "row">
 <div class = "col-md-5">
  <table class="table table-bordered"style="margin-left:20px;">
        <tr>
          <th>ジャンル</th>
          <td></td>
          <td></td>
        </tr>
        <% @genres.each do |genre| %>
          <tr>
            <th><%= genre.name %></th>
            <td>
            <%= link_to "編集する", edit_admin_genre_path(genre.id),class:"btn btn-sm btn-success edit_book_36722" %>
            

        <% end %>
  </table>
  </div>
</div>